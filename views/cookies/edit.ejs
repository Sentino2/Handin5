<!DOCTYPE html>
<html lang="en">
<head>
  <title>Edit Pizza | Pizza Restaurant</title>
  <link rel="stylesheet" href="/styles.css">
  <style>
    body {
      font-family: 'Verdana', sans-serif; /* Clean retro font */
      margin: 0;
      padding: 0;
      background-color: #eaf7f7; /* Soft teal background */
    }

    main {
      padding: 30px;
      max-width: 800px;
      margin: 50px auto;
      background-color: #ffe8c2; /* Warm cream background */
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
      border-radius: 15px;
      border: 4px solid #e63946; /* Muted red border */
      text-align: center;
    }

    h2 {
      color: #e63946; /* Muted red */
      font-size: 28px;
      text-transform: uppercase;
      letter-spacing: 2px;
      margin-bottom: 20px;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 15px;
      align-items: center;
    }

    input[type="text"],
    input[type="number"] {
      padding: 12px;
      border: 2px solid #f4a261; /* Mustard yellow border */
      border-radius: 8px;
      font-size: 16px;
      background-color: #fff; /* Clean white for input fields */
      width: 90%; /* Adjust width for better alignment */
    }

    button {
      padding: 12px 25px;
      background-color: #e63946; /* Muted red */
      color: #ffe8c2; /* Cream text */
      border: none;
      border-radius: 8px;
      font-size: 16px;
      text-transform: uppercase;
      cursor: pointer;
      transition: background-color 0.3s, transform 0.2s;
    }

    button:hover {
      background-color: #c0392b; /* Darker red */
      transform: scale(1.05); /* Slight zoom effect */
    }
  </style>
</head>
<body>
  <%- include('../partials/header') %>

  <main>
    <h2>Edit your Pizza</h2>
    <form action="/cookies/<%= cookie.slug %>" method="POST">
      <input type="text" value="<%= cookie.name %>" name="name" placeholder="Pizza Name" required>
      <input type="text" value="<%= cookie.slug %>" name="slug" placeholder="Pizza Slug" required>
      <input type="number" value="<%= cookie.priceInCents %>" name="priceInCents" placeholder="Price in Cents" required>
      <button>Save</button>
    </form>
  </main>

  <%- include('../partials/footer') %>
</body>
</html>
