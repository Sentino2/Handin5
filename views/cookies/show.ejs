<!DOCTYPE html>
<html lang="en">
<head>
  <title><%= cookie.name %> | Retro Cookieshop</title>
  <link rel="stylesheet" href="/styles.css">
  <style>
    body {
      font-family: 'Verdana', sans-serif; /* Clean retro font */
      margin: 0;
      padding: 0;
      background-color: #eaf7f7; /* Soft teal background */
    }

    main {
      padding: 30px;
      max-width: 800px;
      margin: 50px auto;
      background-color: #ffe8c2; /* Warm cream background */
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
      border-radius: 15px;
      border: 4px solid #e63946; /* Muted red border */
    }

    h1 {
      text-align: center;
      color: #e63946; /* Muted red */
      font-size: 32px;
      text-transform: uppercase;
      letter-spacing: 2px;
      margin-bottom: 20px;
    }

    p {
      font-size: 18px;
      color: #4a342e; /* Dark brown for retro text */
      margin: 15px 0;
    }

    strong {
      color: #e63946; /* Muted red for emphasis */
    }

    .stock-status {
      font-size: 20px;
      font-weight: bold;
      margin-top: 20px;
    }

    .action-links {
      display: flex;
      justify-content: center;
      gap: 20px;
      margin-top: 30px;
    }

    .action-links a {
      padding: 12px 25px;
      background-color: #e63946; /* Muted red */
      color: #ffe8c2; /* Cream text */
      text-decoration: none;
      border-radius: 8px;
      font-weight: bold;
      text-transform: uppercase;
      transition: background-color 0.3s, transform 0.2s;
    }

    .action-links a:hover {
      background-color: #c0392b; /* Darker red */
      transform: scale(1.05); /* Slight zoom effect */
    }
  </style>
</head>
<body>
  <%- include('../partials/header') %>

  <main>
    <h1><%= cookie.name %></h1>
    <p><%= cookie.description %></p>
    <p><strong>Price: <%= readablePrice(cookie.priceInCents) %></strong></p>
    <% if(cookie.isInStock) { %> 
      <p class="stock-status" style="color: green;">In Stock!</p>
    <% } else { %>
      <p class="stock-status" style="color: tomato;">Temporarily Sold Out!</p>
    <% } %>
    <div class="action-links">
      <a href="/cookies/<%= cookie.slug %>/edit">Edit Pizza</a>
      <a href="/cookies/<%= cookie.slug %>/delete">Delete Pizza</a>
    </div>
  </main>

  <%- include('../partials/footer') %>
</body>
</html>
